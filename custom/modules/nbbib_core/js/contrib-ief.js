(function($) {
  'use strict';

  // Formatting and validation tools for contribs inline entity form.
  Drupal.behaviors.contribIEF = {
    attach: function (context, settings) {
      $("button").click( function() {
        alert('test');
      });
    },
  };
})(jQuery);
