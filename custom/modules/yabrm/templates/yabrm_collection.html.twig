{#
/**
 * @file yabrm_collection.html.twig
 * Default theme implementation to present Bibliographic Collection data.
 *
 * This template is used when viewing Bibliographic Collection pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_yabrm_collection()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('yabrm_collection') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
